"use strict";
// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***
Object.defineProperty(exports, "__esModule", { value: true });
exports.RandomUuid4 = void 0;
const pulumi = require("@pulumi/pulumi");
const utilities = require("./utilities");
/**
 * The resource `random.RandomUuid4` generates a random version 4 uuid string that is intended to be used as a unique identifier for other resources.
 *
 * This resource uses [google/uuid](https://github.com/google/uuid) to generate a valid V4 UUID for use with services needing a unique string identifier.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as azurerm from "@pulumi/azurerm";
 * import * as random from "@pulumi/random";
 *
 * // The following example shows how to generate a unique name for an Azure Resource Group.
 * const test = new random.RandomUuid4("test", {});
 * const testResourceGroup = new azurerm.index.ResourceGroup("test", {
 *     name: `${test.result}-rg`,
 *     location: "Central US",
 * });
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * Random UUID's can be imported. This can be used to replace a config
 *
 * value with a value interpolated from the random provider without
 *
 * experiencing diffs.
 *
 * ```sh
 * $ pulumi import random:index/randomUuid4:RandomUuid4 main 7e4436da-7c71-486e-a57c-830b25fff7bd
 * ```
 */
class RandomUuid4 extends pulumi.CustomResource {
    /**
     * Get an existing RandomUuid4 resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    static get(name, id, state, opts) {
        return new RandomUuid4(name, state, { ...opts, id: id });
    }
    /**
     * Returns true if the given object is an instance of RandomUuid4.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    static isInstance(obj) {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === RandomUuid4.__pulumiType;
    }
    constructor(name, argsOrState, opts) {
        let resourceInputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState;
            resourceInputs["keepers"] = state?.keepers;
            resourceInputs["result"] = state?.result;
        }
        else {
            const args = argsOrState;
            resourceInputs["keepers"] = args?.keepers;
            resourceInputs["result"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(RandomUuid4.__pulumiType, name, resourceInputs, opts);
    }
}
exports.RandomUuid4 = RandomUuid4;
/** @internal */
RandomUuid4.__pulumiType = 'random:index/randomUuid4:RandomUuid4';
//# sourceMappingURL=randomUuid4.js.map